<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permission to access the internet for Firebase -->
    <uses-permission android:name="android.permission.INTERNET" />
    
    <!-- Permission to access network state -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Permission to read external storage for selecting images from gallery -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    
    <!-- Permission to use the camera for taking plant photos -->
    <uses-permission android:name="android.permission.CAMERA" />
    
    <!-- Camera feature declaration (not required, but recommended) -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:name=".YanaghApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Yanagh"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="false">
        
        <!-- FileProvider for camera image capture -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        
        <!-- Login Activity - Entry point for unauthenticated users -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.Yanagh">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- Sign Up Activity for new user registration -->
        <activity
            android:name=".SignUpActivity"
            android:exported="false"
            android:theme="@style/Theme.Yanagh" />
        
        <!-- Food home (default after login) -->
        <activity
            android:name=".FoodMainActivity"
            android:exported="false"
            android:theme="@style/Theme.Yanagh" />
        <activity android:name=".OnboardingActivity" android:exported="false" />
        <activity android:name=".SearchActivity" android:exported="false" />
        <activity android:name=".DietActivity" android:exported="false" />
        <activity android:name=".FavoritesActivity" android:exported="false" />
        <activity android:name=".SettingsActivity" android:exported="false" />
        <activity android:name=".RecipeDetailActivity" android:exported="false" />
        <activity android:name=".ShoppingListActivity" android:exported="false" />
        
        <!-- Profile and Edit Profile -->
        <activity
            android:name=".ProfileActivity"
            android:exported="false"
            android:theme="@style/Theme.Yanagh" />
        <activity
            android:name=".EditProfileActivity"
            android:exported="false"
            android:theme="@style/Theme.Yanagh" />
        <!-- Food assistant -->
        <activity
            android:name=".AnalyzeFoodActivity"
            android:exported="false" />
        <activity
            android:name=".FoodAnalysisChatActivity"
            android:exported="false" />
        <activity
            android:name=".FoodChatActivity"
            android:exported="false" />
    </application>

</manifest>